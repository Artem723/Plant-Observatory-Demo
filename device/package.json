{
  "name": "device",
  "version": "1.0.0",
  "description": "device application that sends telemetry",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "azure-iot-device": "^1.7.1",
    "azure-iot-device-mqtt": "^1.7.1"
  }
}
